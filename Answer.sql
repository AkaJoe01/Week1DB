Create database SalesDB (
    SalesID int,
    SalesDate date,
    SalesAmount float,
    SalesTax float,
    SalesTotal float,
    SalesComments varchar(255),
);

Create tabele SalesDB.Sales (
    SalesID int,
    SalesDate date,
    SalesAmount float,
    SalesTax float,
    SalesTotal float,
    SalesComments varchar(255)
);


Drop database demo (

);

